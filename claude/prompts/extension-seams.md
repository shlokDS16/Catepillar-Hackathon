# Prompt: Extension Seams

Identify every point where this system can be extended without modifying core code.

For each seam report:
- where it is
- what kind of change it absorbs
- what it cannot absorb
- how much it costs to use
- whether it is currently load-bearing

Then name the change most likely to be requested next, and say whether an existing
seam can carry it.
