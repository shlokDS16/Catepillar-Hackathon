# Gate G2: build plan validation (phase-validator)

- **Date:** 2026-09-23 · **Validator:** phase-validator (Opus 5.5, fresh context)
- **Scope:** the complete build plan before two parallel Fable 5.1 sessions start (Track B, Track F).
- **Inputs read:** spec v3 (`docs/specs/idea.md`), `decisions.md` D1-D10, ADR-001 rev 3 with its disposition tables,
  `api-contracts.md`, `data-model.md`, `event-pipeline.md`, `backend-tasks.md`, `docs/design/*.md` v2,
  `docs/gates/G1-*.md`, `G2-backend-review.md`, `G2-program.md`, `RESUME.md`, `STATE.md`, `CHECKLIST.md`,
  `.env.example`, the variable names in `.env` (no values read out), `.claude/agents/*`, `git status`.
- Abbreviations: AC api-contracts, DM data-model, EP event-pipeline, BT backend-tasks, FT frontend-tasks,
  IM interaction-map, SC screens.

## Verdict: **FAIL** (7 blocking fixes; about 1 h of document work and 4 decisions from Shlok, no redesign)

The architecture and contracts are sound. Every earlier blocker and major finding has a disposition, and the
contract names line up across the documents. The **build launch** is what fails. The resume prompts contradict
the working agreement on shared files, lanes and merges. STATE.md is stale. CHECKLIST.md has no track task IDs.
Two P0 items have no owner. A fresh Fable session following prompt B or prompt F literally would stall or
create merge conflicts in its first hours.

---

## 1. Traceability (spec §5 P0 and §7 demo steps → tasks)

| Spec P0 item | Track B | Track F | Status |
|---|---|---|---|
| M1 Hero card, progress, ETA P50/P90 | B9 (progress), B16 | F06, F08 | ✓ |
| M1 Tasks Start/Pause/Complete | B15 | F10 | ✓ |
| M1 Conditions chip (WBGT, wind chill) | B7, B4 snapshot | F08 | ✓ |
| M2 Seatbelt / proximity / PPE + override / motion lock / alert budget | B10, B9, B15, B12 | F11, F10, F14, F06, F04 | ✓. Radar and machine↔machine cut (needs Shlok's approval, §7) |
| M2 Four tiers + vibration + **pre-generated Hindi audio** | B12 | F04 | **Orphan: nothing generates the clips.** BT §4 lists only the provider; UI-17 names "the TTS owner (ADR-002)", and ADR-002 is not decided |
| M2 SOS (Telegram, inbox, 60 s Twilio escalation) | B12, B13, B14, B15 | F05, F14 | ✓ |
| M3 Log incident, hash chain, Verify, Merkle to Telegram | B5, B13, B15 | F11, F15 | ✓. Daily cron cut; checkpoint kept |
| M4 Map (trail, zones, health) + Guardian + call | B11, B13, B14 | F12 | ✓. The anomaly table is cut in FT §4.1 #6 |
| M5 Rules + per-machine EWMA + measured P/R + one live anomaly | B10, B11, B17, B6 | F12, F20 | ✓. Three rules and the fleet EWMA are cut |
| M5 **Plain-language explanation + cost** ("idled 58 %, 2.3× normal, ≈14 L ≈ ₹1,300") | none | none | **Orphan.** `AnomalyDetected` (AC:100-102) and `anomalies` (DM §2.5) carry only `features`. SC:93 shows "explanation and cost" with no data source |
| M6 ETA model + "why" + MAE evidence | B16, B17 | F10, F20 | ✓. What-if slider cut (FT §4.1 #3) |
| M6 Supervisor analytics (task × condition, bias) | B2 view, B16 | none | **Inconsistent.** BT §3 IP5 promises "one analytics chart", but FT §4.1 #4 cuts it |
| M7 Replay (four phases), Loop, micro-lessons | B10b, B8, B15 | R1-R4, F08, F17 | ✓ (seatbelt only) |
| M8 Ask (hybrid RAG, roles, tools, safety pattern, injection defence) | B18, B19 | F19 | ✓. Eval shown as "in progress" |
| M9 Chip + last synced + cached snapshot | none | F13 | ✓ |
| M10 First launch, en+hi, Simple/Detailed, glove targets, privacy | B15 (`pair_machine`, `consent_set`, `near_miss_list`), B2 RLS | F01, F02, F08, F09, F11 | ✓. Tamil and "My data" cut |
| M11 Inbox, ledger, evidence, fleet map | B12, B5, B17 | F14, F15, F20, F12 | ✓ |
| M0 Clock, scenarios, director, events spine | B9, B6, B15, B22, B4 | F16, F07 | ✓ |

| Demo step (§7) | Tasks | Status |
|---|---|---|
| 1 Hindi, PPE block, vest, ETA + why | B6, B15, B16 · F09, F10 | ✓ |
| 2 Seatbelt → Warning, vibration, Hindi TTS, ack, ledger | B10, B12, B5 · F04, F11, F18 | ✓, except the audio clips (orphan above) |
| 3 Guardian → map, protocol card, Hindi call, Telegram | B11, B13, B14 · F12 | ✓ |
| 4 Replay of "his own near-miss", then "the 60 s lesson clip" | B10b · R1-R4, F17 | **The spec is stale.** P0 replays the seatbelt breach (BT §5 cut 7, AC §5), and video is P1 (D10). The ADR's PA-1 row still says "Guardian near-miss" |
| 5 Evidence → Verify → tamper → "the Telegram witness hash proves it" | B5, B13, B17 · F15, F16, F20 | **The spec over-claims.** The design now claims only "externally witnessed, human-verifiable" (N1, DM §4.5) |
| 6 Close | pitch (P2) | n/a |

**Orphans:** the M5 explanation and cost; the Hindi clip generation. The supervisor analytics chart has contradictory owners.

## 2. Contracts (spot-check of 10 names)

| Name | AC | DM / EP / BT | FT / IM / SC | Result |
|---|---|---|---|---|
| `ReplayScenario` (seatbelt, four phases) | §5 | DM §2.8, BT B10b | R1-R4 | ✓ |
| `ReplayDebriefResult.scores {safety, procedure, efficiency}` vs `training.replay_completed {outcome_score, process_score}` | §5 vs :130 | none | FT UI-19 | **✗ drift.** Fix in B1 before the freeze; after the freeze it is a breaking change |
| `LedgerRecomputeIn/Out`, `ledger_recompute` | :245-247 | BT B5, B15; DM §4.5 prose only (no signature) | F15, IM:256 | ✓ (minor: no SQL signature in DM) |
| `WITNESS_LINE` regex vs checkpoint text | :248 | DM §4.5 message | F15 | ✓. The regex has no end anchor, so the trailing `at=` is accepted |
| `incident.reported` (ledger) / `incident.logged` | :177-178 | DM §4.2, EP:13 | F11, IM:139 | ✓ |
| `operator_state.motion_locked`, `nearest`; `operator.motion_lock_changed` | §4 snapshot | DM, EP §2 | F06, F11, SC:81 | ✓ |
| `EvidenceKey` `loop.repeat_rate.assigned/control`, `fleet.idle_pct` | §8 | BT B17 | F20, SC:188 | ✓ |
| `ReplaySubmitIn.p_choices[].choice_ids.min(1)` | §4 | scoring in DM §2.8 | R3 (omits timed-out steps) | Partial: the scoring semantics are unconfirmed (UI-18) |
| `DEMO_DRIVER_SECRET` for `demo-login` | §6 | EP §10, BT B22 | F09, UI-21 | Partial: B0b does not put the variable into Vercel |
| `ASSUMED_FIELDS` ("assumed" tags) | **not defined** (only the file name `contracts/assumed.ts`, AC:33) | none | SC:11 | **✗.** B1 cannot produce it "exactly as api-contracts rev 3" |

**UI-17 to UI-22 have no owner.** AC §10 closes only UI-1 to UI-16. FT §5 gives an interim UI behaviour for each question but no owner and no deadline.

## 3. Earlier findings

- **G2-backend-review #1-#21:** all dispositioned (ADR:134-156). The reviewer's re-check marked #1-#3 and #5-#21 FIXED and #4 PARTIAL.
- **Re-check N1-N5, #4 PARTIAL, #15/#19 residuals:** dispositioned in ADR rev 3 (ADR:173-189). N1 is resolved by
  removing the automated witness. N2 is resolved by the `loop_queue` step. N3 is resolved by a single ledger path and a lock on
  every mutator. N4 is accepted with a backstop and a probe. N5 is resolved with two jobs.
  **These dispositions are author-asserted. No backend-reviewer has re-checked rev 3.** Rev 3 introduces a new
  mechanism: a `worker` procedure that COMMITs between steps, called by pg_cron. DM §6 marks the COMMIT semantics as [A].
- **G2-program PA-1 to PA-10:** all dispositioned (ADR:157-166). Three are still open:
  - PA-4 (the lane amendment) awaits Shlok.
  - PA-9 (G3 definition) was handed to the orchestrator, but G3 is not in CHECKLIST.
  - PA-6's row still says "otherwise the demo runs from localhost". The rev 3 "Deploy P0" row and BT §0 say localhost is not an option. Rev 3 supersedes PA-6, but PA-6 is not marked as superseded.
- **G1:** the scope blocker was handled by the G2 cut lists. The spec still lists the cut items as P0 (§7, fix 7).

## 4. Parallel build safety

| Rule | Evidence | Problem |
|---|---|---|
| STATE/CHECKLIST ownership | BT §0.2: "only the orchestrator edits CHECKLIST.md and STATE.md". RESUME:33-35 and :46-47 tell **both tracks** to tick CHECKLIST and "Update STATE.md at every task boundary". backend-lead.md:32 says "record them in CHECKLIST.md" | Direct contradiction. Both branches editing the same STATE.md lines conflicts at every IP merge |
| Merges to `main` | BT §0.3: Track B merges to main. FT §0.3(d) and UI-22: the orchestrator merges Track F | `main` is checked out in the primary folder, so git refuses `git checkout main` in either worktree. Three merging actors are named and none has a mechanism. The Vercel deploy "from main" (B0b, every IP) must run in the folder holding `main`. `.vercel/` is git-ignored, so a link made in `track-b` does not carry over |
| H0 dependency commit | BT §0.1: "one commit installs every known dependency for both tracks … before the worktrees are created" | No dependency list exists anywhere. RESUME's H0 block does not include this step, `pnpm install` per worktree (node_modules is not shared), or a rule for new dependencies after H0 (the lockfile would conflict) |
| `.env` | RESUME copies the root `.env` | Next.js reads `apps/web/.env.local`. Nobody creates it at H0, even though F09 needs `DEMO_DRIVER_SECRET` server-side (UI-21). `CONNECTION_STRING` (session pooler, used by `sqltest`) is **absent from `.env`** |
| Cross-track messages | BT §0.6: destructive DB operations are "announced in both logs". AC §0.1: Track F puts contract requests in its log | Each log lives in its own branch, so the other track cannot see it until a merge. There is no channel between the sessions |
| Deploy at H0 | BT §0 B0b; PW.6 is still `[ ]` (`vercel login` broken) | No fallback if the login fails at H0. Viable: connect the Vercel project to the GitHub repo in the dashboard, so a push to `main` deploys (git push works, D5) |
| Lanes | BT §0: subagent lane B6-B8 must land by IP1 (≈H5.5). B9's acceptance needs the scenario | RESUME B: "Execute tasks strictly in order, one at a time". Read cold, B6-B8 never run before B9, and IP1 (seeded logins) slips |

**Does CHECKLIST.md need Track B and Track F sections?** Yes. It has only a generic "P4.x" line, so RESUME's "tick the task in CHECKLIST.md (Track B section only)" points at a section that does not exist. Even if only the orchestrator ticks, the task IDs, IP0-IP6, G3 and the dry runs must be listed for the gate to be checkable.

## 5. Resume prompts (cold-start test)

- The task IDs exist: **B0** (BT:57) and **F01** (FT:31). ✓ The agent files exist and are tracked in git. ✓
- A cold session reads STATE.md first. STATE is stale:
  - It says "Phase: P3", "In flight: backend-lead writing docs/architecture".
  - Its next action is a review that has already happened.
  - It lists open decisions that D8 and D9 superseded.
  - It never mentions G2, H0 or the IPs.
- Prompt B has three problems:
  - The lane conflict above.
  - "DRY_RUN off" names a flag that does not exist. EP uses `dry_run_external` on runs.
  - It does not say where the session merges or deploys.
- Prompt F has four problems:
  - "Touching only apps/web" conflicts with FT's own writes to `docs/design/qa/` and `docs/sessions/track-f.md`.
  - Screenshot widths: the prompt says 375/768/1440, FT says 375/1440.
  - It never reads `api-contracts.md`.
  - It gives no rule for a task whose "Needs" IP has not landed on `main`. Under "strictly in order", the session would stall.
- The agent files contradict the plan:
  - backend-lead.md:19,23 says "offline included" and "Offline-first is a hard requirement". Spec M9 makes offline P1/P2.
  - ui-ux-lead.md asks for "light and dark" screenshots. The spec's default is light, and the night theme is P1.
  - Neither prompt says that the task documents override the agent-file defaults.

## 6. Honesty rules (spec §3) in the design docs

| Rule | Carried? | Evidence |
|---|---|---|
| "Assumed sensor" tag on every non-organiser field | Partly | SC:11, SC:19, SC:73 and `EtaFactor.assumed` (AC:117). `ASSUMED_FIELDS` is undefined (see §2) |
| Every screen labelled simulated | ✓ | "SIM" in the connectivity chip (IM:52); evidence title "simulated data · synthetic cohort" (SC:183) |
| Repeat-event chart titled "how we would measure impact" | ✓ | SC:188; FT F20 "assigned vs control" |
| Tamper-**evident**, human-verifiable witness | Partly | "Externally witnessed, human-verifiable" appears in IM:262, SC:203, DM §4.5. The word **"tamper-evident" appears in no design doc**, and spec §7 step 5 still says the Telegram hash "proves it" |
| Weather as a forecast | ✓ | SC:60 "peak 44 °C forecast by 14:00", labelled "scenario forecast" |
| "Informed by ISO 21815; not certified" / "instructor sign-off required" | Not in design | Needed on the Guardian protocol card or the About sheet and in the deck. Deck-level is acceptable if P2 owns it |

## 7. Fixes

| # | Fix | Where | Blocking |
|---|---|---|---|
| 1 | **Pick one owner for STATE/CHECKLIST.** Recommended: tracks write only `docs/sessions/track-{b,f}.md`; the orchestrator (integrator) ticks CHECKLIST and updates STATE on `main`. Rewrite both RESUME prompts to match. Add to each prompt: "the task documents override defaults in the agent files" | RESUME §B, BT §0.2 | **Yes** |
| 2 | **Name one integrator and write out the mechanism.** The orchestrator session in the primary folder (on `main`) runs `git merge track-b` / `git merge track-f` at each IP (Track F's `haptics-test.html` goes before B0b), then `vercel deploy --prod` from that folder. Both tracks then run `git merge main`. Rewrite BT §0.3, FT §0.3(d), UI-22 and B0b to match | RESUME, BT §0, FT §0 | **Yes** |
| 3 | **Decide the lanes.** Shlok approves or rejects PA-4. Prompt B states the order explicitly: main lane B0, B0b, B1, B2, B4, B5, B9, B10, B10b, B11, B12, B15, B13, B14, B21; subagent lane (Fable subagents) B6 → B7 → B8 (by IP1), B16, B17, B18 → B19, B22. If PA-4 is rejected, re-baseline Track B (≈20.5 h serial) | RESUME §B, BT §0 | **Yes** |
| 4 | **Write an H0 runbook in RESUME.** (a) Shlok: `vercel login`, or the GitHub-integration fallback. (b) Dependency commit on `main`, with a written, pinned list (web: next-intl, maplibre-gl, @supabase/supabase-js, vitest, …; B: postgres, vitest, …). (c) Worktrees at absolute paths. (d) `pnpm install` in each worktree. (e) Copy `.env`, create `apps/web/.env.local` (the `NEXT_PUBLIC_*` values plus `DEMO_DRIVER_SECRET` server-side) and add `CONNECTION_STRING`. (f) Create `docs/sessions/`. (g) Rule: no new dependency after H0 except through the integrator on `main` | RESUME | **Yes** |
| 5 | **Update STATE.md and CHECKLIST.md.** STATE: phase P4, G2 verdict, H0 steps, current open decisions. CHECKLIST: G2 gate line, close or supersede P3.1c/P3.2/P3.4, and add P4 Track B and Track F sections with every task ID, IP0-IP6, G3 (≈H6, fixtures + seeded logins, PA-9) and dry runs #1 and #2 | STATE.md, CHECKLIST.md | **Yes** |
| 6 | **Give the two orphans an owner.** (a) M5 explanation + cost: add `explanation: I18nText`, `fuel_l`, `cost_inr` to `AnomalyDetected`/`anomalies` in B1 and to the B10/B11 acceptance criteria, or have Shlok cut it formally. (b) Hindi clips: add a subagent-lane task (for example B23, `scripts/tts-clips.ts`, alert, lesson and replay audio under the UI-17 path convention, by H11) once the TTS provider is chosen | AC §3, BT §1-2 | **Yes** |
| 7 | **Shlok approves the cut lists** (BT §5, 12 items; FT §4.1, 13 items, including the what-if slider, the anomaly table, analytics, Tamil, the radar = FT decision 1, and "My data"). Settle whether IP5 delivers an analytics chart. Record everything in a new D11 and a spec v3.1 §5/§7 amendment. Step 4 becomes the seatbelt replay + card lesson; step 5 becomes "externally witnessed, human-verifiable" | decisions.md, idea.md | **Yes** (decision) |
| 8 | Close UI-17 to UI-22 with owners. UI-17 → B23. UI-18, UI-19, UI-20 → Track B in B1 (contract) and B10b/B15 (semantics), answered in the log by IP0. UI-21 → B0b (the Vercel server env). UI-22 → the integrator | AC §10, FT §5 | No (must land in B1) |
| 9 | In B1, fix the `training.replay_completed` payload to carry `ReplayDebriefResult` scores and define `ASSUMED_FIELDS` (the organiser's 9 + 7 fields vs assumed) | AC §3, §1 | No (must land in B1) |
| 10 | Add a backend-reviewer delta check of rev 3 (the worker procedure, N1-N5), about 20 min, before B9. In B9, prove first that the pg_cron `CALL` + in-procedure COMMIT works; the fallback is one job per step | BT B9 | No |
| 11 | Clean the stale entries in BT §4: "organiser sample dataset blocks B6" (contradicts D8); "Gemini key not in .env" (it is set); the duplicated TTS row. In spec §10, remove "built from the organiser's sample". In ADR, mark the PA-1 and PA-6 rows as superseded and set status Accepted. Mark D4 decided. Record the D7 vs D9 (Gemini) and D7 vs ADR (Sarvam) conflicts | BT, idea.md, ADR, decisions.md | No |
| 12 | In prompt F: allow writes to `docs/design/qa/` and `docs/sessions/track-f.md`; align the screenshot widths; add `api-contracts.md` §3-§5 and §7 to the read list; add the rule "if a task's IP is not on main, take the next ready task and log it". In prompt B: define the dry-run flag name | RESUME | No |
| 13 | Put "tamper-evident" and "not a certified collision-warning system" into the Ledger footer and the Guardian card/About sheet, or assign them to the deck (P2) | SC S9, S4 | No |

## 8. Decisions for Shlok (before H0)

1. Lane amendment PA-4 (subagents in `scripts/`, `functions/ask`, `functions/demo-login`): yes or no.
2. Cut lists BT §5 and FT §4.1, plus FT decisions 1-3 (map rings instead of the radar, untimed Brief, training in ink).
3. M5 explanation + cost: build it (≈0.5 h) or cut it.
4. Hindi TTS provider, by H6. The Sarvam keys are **not in `.env`** (they are marked REQ in `.env.example`). Gemini TTS Hindi quality is unverified. Azure F0 is the alternative.
5. `vercel login` at H0, or the GitHub-integration fallback.
6. Source of the RAG corpus for `docs/brief/data/kb/` (the folder does not exist; it blocks B18 at ≈H3).
7. Twilio: verify both demo phones and India geo permissions; Anita presses Start on the bot (B0 smoke test).
8. Confirm the orchestrator session stays open as the integrator for the whole build.

---

## Re-run (2026-09-23, after commits a646376..ace7c67)

**Verdict: FAIL (narrow).** All 7 first-pass fixes are addressed in the documents. The H0 setup claimed as done
is incomplete in three ways, each a few minutes of integrator work. Once R1-R3 are done and the listed checks
pass, this becomes **PASS-WITH-FIXES** with no further validator run. R4-R9 are non-blocking.

**Correction to the first pass:** the Sarvam keys *are* in `.env`. My check missed them because the lines are
written `SARVAM_API_KEY_1 =`, with a space before `=`.

### The 7 fixes, verified
| # | Verified | Evidence |
|---|---|---|
| 1 STATE/CHECKLIST owner | ✓ | build-protocol.md "Roles"; RESUME prompts B and F say "never CHECKLIST.md or STATE.md"; BT §0.2 |
| 2 Integrator + merge mechanism | ✓ | build-protocol "Merging"; BT §0.3 (`git merge track-f`, then `track-b`, then `vercel deploy --prod` from the main folder); AC §10 UI-22 |
| 3 Lanes | ✓ | D11 approves the lane; BT §6 has the graph plus the main-lane and subagent-lane order; FT §1.1 has the edge list and the "next ready task" rule (FT:21) |
| 4 H0 runbook | **Partial** | Worktrees exist on `track-b`/`track-f`. `.env` and `apps/web/.env.local` are in both. `CONNECTION_STRING` is set. `vercel whoami` returns `shlokds16`. `pnpm --filter web build` is green in `spotter-track-f` (re-run here). **The dependency commit is missing**, see R1 |
| 5 STATE/CHECKLIST | Partial (non-blocking) | CHECKLIST has P4 track sections, but as placeholders ("B0 → …"). STATE still says "Phase P3 … fixes in progress" (R5) |
| 6 Orphans | ✓ | M5: AC §12 `EXPLANATION_TEMPLATES` and `cost_inr`, BT B10. Audio: B23 plus AC §13 `PHRASES`/`AUDIO_MANIFEST`. Analytics: FT F20 "the P0 chart", BT IP5 |
| 7 Cuts + spec | ✓ | D11 (cut lists, radar → map rings, UI decisions). Spec §7 step 4 is now the seatbelt Replay + card lesson; step 5 says "tamper-evident, human-verifiable" |

The contract fixes from the first pass (items 8, 9 and 13) are also closed:
- `ReplayCompleted` carries `ReplayScores` (AC:137).
- `FIELD_PROVENANCE` replaces `ASSUMED_FIELDS` (AC §11; SC:4 and :17).
- UI-17 to UI-22 each have an owner (AC:521-526).
- "Not a certified collision-warning system" is on SC:97.
- Re-check 2: the B0 COMMIT probe plus its fallback, B5 `ledger_export` with the browser `verifyLedger`, and B24c are all in BT.

### Blocking
- **R1. The H0 dependency commit was not made.** Evidence:
  - Commit 435ff70 changed only CHECKLIST.md and screens.md.
  - Root `package.json` has no `vitest` or `tsx`.
  - `apps/web/package.json` has no next-intl, maplibre-gl or @supabase/supabase-js.
  - There is no `scripts/` workspace package, and `pnpm-workspace.yaml` does not list one.
  - build-protocol.md's H0 steps 1-6 leave this step out.

  The consequence: both tracks will `pnpm add` on their own branches, and `pnpm-lock.yaml` will conflict at the first merge (PA-5). **Fix:** the integrator installs BT §6's list plus Track F's list on `main` and commits. Add this as a step in build-protocol H0.
- **R2. Both worktrees are 3 commits behind `main`.** They sit at 74f91af; `main` is at ace7c67. A cold session would read the old backend-tasks.md (no COMMIT probe, no `ledger_export`, no B24c), the old frontend-tasks.md, and an empty handoff.md. **Fix:** after R1, run `git -C ../spotter-track-b merge main` and the same for track-f (both are fast-forwards), then `pnpm install` in each. Add "run `git merge main` before your first task" to prompts B and F.
- **R3. Two required secrets are empty in `.env`** and therefore in all copies: `DEMO_DRIVER_SECRET` (needed by B0b in Vercel, by `director` and by F09's route handler) and `TELEGRAM_WEBHOOK_SECRET` (needed by B0 secrets and B13). No task generates them. **Fix:** generate two random values once, in the main `.env`, then re-copy to both worktrees and both `apps/web/.env.local` files.

Checks after R1-R3:
- `git -C ../spotter-track-{b,f} log -1` equals `main`.
- Both `pnpm-lock.yaml` files equal `main`'s.
- Both secrets are non-empty in all four env files.

### Non-blocking
- **R4. F15 still uses the server check.** F15 (FT:56) and IM:257 still compare against the server's `ledger_recompute`. AC:252 and DM §4.5 now require the browser `verifyLedger` on the `ledger_export` rows. The only correction is a handoff note that prompt F never tells the session to read. Fix F15 and IM, add `B5 (IP1)` to F15's dependencies, and add "read docs/sessions/handoff.md at start and after every merge" to both prompts.
- **R5. STATE.md must be updated at launch:** phase P4, G2 verdict, "Track B starts B0, Track F starts F01", integrator open. Both prompts read STATE first. Prompt A still says "D1-D10"; it should say D11.
- **R6. `handoff.md` is appended on both branches**, so every merge conflicts at the end of the file. Add `docs/sessions/handoff.md merge=union` to `.gitattributes`.
- **R7. `.env` lines with a space before `=`** (`SARVAM_API_KEY_*`, `CONNECTION_STRING`): normalise them to `KEY=value`. Whether `supabase secrets set --env-file` accepts the spaced form is [U].
- **R8. `apps/web/.env.local` is a copy of the full `.env`,** including `SUPABASE_SECRET_KEY`, `TWILIO_AUTH_TOKEN` and the other provider keys. Next.js will not expose them to the client, but keep only the `NEXT_PUBLIC_*` values and `DEMO_DRIVER_SECRET` (least privilege).
- **R9. First-pass carry-overs:**
  - Prompt B's "DRY_RUN" still has no defined variable name; the design uses `dry_run_external` on runs.
  - backend-lead.md still says "Offline-first is a hard requirement".
  - CHECKLIST's track sections list no task IDs.

### Cold read of prompts B and F (after R2)
Both prompts are unambiguous:
- start task (B0 exists at BT:69; F01 at FT:36);
- reading order;
- dependency order;
- the folders each track may touch (Track F's exceptions are at FT:19);
- review and commit steps;
- log and handoff files;
- "never CHECKLIST/STATE".

Two gaps remain: the `git merge main` step (R2) and reading handoff.md (R4).

## Integrator confirmation of re-run blockers (2026-09-23) → verdict PASS-WITH-FIXES
1. Shared dependency commit on main: vitest, tsx (root); postgres, @supabase/supabase-js (scripts workspace); next-intl, maplibre-gl, @supabase/supabase-js, @supabase/ssr (apps/web). Web build green. ✓
2. Worktrees merged main: track-b and track-f at 1473244, 0 commits behind; pnpm install --frozen-lockfile ok. ✓
3. TELEGRAM_WEBHOOK_SECRET and DEMO_DRIVER_SECRET generated, copied to all env files (values never printed). ✓
Non-blocking: prompt F now says read handoff.md first ([B→F] items override, F15 = browser verifier); STATE.md updated to P4.
