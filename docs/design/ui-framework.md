# UI framework — PLACEHOLDER (Shlok to paste)

Paste the full ChatGPT-derived UI framework here verbatim, plus the reference site URL(s) it was
derived from. The `ui-ux-lead` agent treats this file as the source of truth and will stop if it
is still a placeholder.

Once pasted, ui-ux-lead derives design tokens into `packages/shared/src/tokens.ts` (colors,
type scale, spacing, radii, motion) so web (Tailwind theme) and mobile (Expo) use one source.
