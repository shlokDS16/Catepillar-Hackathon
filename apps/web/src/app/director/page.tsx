import { DirectorPanel } from "./director-panel";

export const metadata = { title: "Spotter · director" };

/** S10 Director (hidden, English only). Fixture beats now; DirectorRequest to the Edge Function at F16 live. */
export default function DirectorPage() {
  return <DirectorPanel />;
}
